import { default as characters } from "../output/characters.json" with { type: "json" };
import { default as entities } from "../output/entities.json" with { type: "json" };
import { default as actionCards } from "../output/action_cards.json" with { type: "json" };
import { default as keywords } from "../output/keywords.json" with { type: "json" };
export { characters, entities, actionCards, keywords };
